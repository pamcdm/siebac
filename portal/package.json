{
  "name": "portal",
  "version": "0.0.0",
  "description": "SIEBAC Portal",
  "main": "index.js",
  "scripts": {
    "test": "mocha test/*.js",
    "build:site": "gulp build",
    "build:server": "cp src/app.js dist",
    "build": "npm run build:site && npm run build:server",
    "clean": "rm -r dist & rm -r build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "~4.12.0"
  },
  "devDependencies": {
    "mocha": "~2.1.0",
    "supertest": "~0.15.0",
    "bower": "~1.3.12",
    "browserify": "~9.0.3",
    "gulp": "~3.8.11",
    "bower-files": "~3.4.4",
    "gulp-sass": "~1.3.3"
  }
}
